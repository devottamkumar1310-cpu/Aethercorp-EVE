[pytest]
pythonpath = .
# Collection is pinned to tests/ so a stray top-level test_*.py script can never
# be imported by a bare `pytest` run. Scripts that talk to live services have
# been removed, but the guard keeps the blast radius small if one reappears.
testpaths = tests
